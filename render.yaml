version: 1
services:
  - type: web
    name: onlinequiz
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn onlinequiz.wsgi:application"
    autoDeploy: true
